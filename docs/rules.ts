const content = {
  en: `Rules automatically assign classifications based on element properties.

- Define conditions using any available property and operator.
- Each rule targets a classification and can be toggled active or inactive.
- Use **Preview Rule Impact** to highlight elements that match before applying.
- Import or export rule sets as **JSON** or **Excel**.
- Load the provided eBKP‑H rule set from the menu when needed.
`,
  fr: `Rules automatically assign classifications based on element properties.

- Define conditions using any available property and operator.
- Each rule targets a classification and can be toggled active or inactive.
- Use **Preview Rule Impact** to highlight elements that match before applying.
- Import or export rule sets as **JSON** or **Excel**.
- Load the provided eBKP‑H rule set from the menu when needed.
`,
  it: `Rules automatically assign classifications based on element properties.

- Define conditions using any available property and operator.
- Each rule targets a classification and can be toggled active or inactive.
- Use **Preview Rule Impact** to highlight elements that match before applying.
- Import or export rule sets as **JSON** or **Excel**.
- Load the provided eBKP‑H rule set from the menu when needed.
`,
  de: `Regeln ordnen Klassifizierungen automatisch anhand von Element-Eigenschaften zu.

- Bedingungen können mit beliebigen Eigenschaften und Operatoren definiert werden.
- Jede Regel zielt auf eine Klassifizierung und kann aktiviert oder deaktiviert werden.
- Mit **Preview Rule Impact** siehst du vorab, welche Elemente betroffen sind.
- Regelsets lassen sich als **JSON** oder **Excel** importieren und exportieren.
- Das eBKP‑H-Regelset kann bei Bedarf aus dem Menü geladen werden.
`};
export default content;
